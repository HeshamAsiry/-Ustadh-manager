import { NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!u||!k)throw new Error("Supabase environment variables are missing");return createClient(u,k);}
export async function GET(req:Request){try{const url=new URL(req.url),from=url.searchParams.get("from"),to=url.searchParams.get("to");let q=db().from("lessons").select("*,students(id,name,country,timezone)").order("starts_at");if(from)q=q.gte("starts_at",from);if(to)q=q.lt("starts_at",to);const {data,error}=await q;if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Server error"},{status:500});}}
export async function POST(req:Request){try{const body=await req.json();const {data,error}=await db().from("lessons").insert(body).select("*,students(id,name,country,timezone)").single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data},{status:201});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Server error"},{status:500});}}
